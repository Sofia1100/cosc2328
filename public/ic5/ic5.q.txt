What does an external stylesheet do, and why is keeping CSS in its own file better than putting styles inside the HTML?
It would be better to keep it in its own file because it would be more organized for the programmer
for having all the stylitic choices in one file.
In a CSS rule, what is the selector and what is the declaration (property: value;)? Give one example from your file.
A selector would be the particular element you would want to style while the declaration would be more of the 
attributes such as the property and value of that element. An example for this in my file would be
h1 I would think.
What is the difference between a class selector (.card) and an id selector (#intro), and when would you use each?
The difference between a class selector and and id selector is that a class selector would be able to style multiple
elements while a id selector would only be able to still one unique element.
Both the <link> (css/ic5.css) and the <img> (images/photo.jpg) use relative paths. What does each one point to, relative to where ic5.html lives?
The link in css/ic5 would point to to a folder name css and looks for a file ic5 within in.
The link for images would point to the image folder and the image(s) within it.